---
title: Using Databases in R
weight: 9
---


```{r setup, include=FALSE}
library(htmltools)

thumbnail <- function(title, img, href, caption = TRUE) {
  div(class = "col-sm-3",
      a(class = "thumbnail", title = title, href = href,
        img(src = img),
        div(class = ifelse(caption, "caption", ""),
          ifelse(caption, title, ""), align="center")))}

```


```{r, echo=FALSE}
div(class = "col-sm-12", br())
```

We will review common scenarios that require database connectivity. 

###Simple example

```{r, echo=FALSE}
div(class = "col-sm-10","A simple example that covers all the steps for implementing a database data source in R.")
thumbnail("Simple example", "conn-setup.png", "simple")
```

